import { BottomNavSheila } from "./BottomNavSheila";

/**
 * شريط تنقّل سفلي بنمط Liquid Glass — يعتمد التصميم الجديد (BottomNavSheila).
 */
export function BottomNav(_props?: { activeTab?: string }) {
  return <BottomNavSheila />;
}
